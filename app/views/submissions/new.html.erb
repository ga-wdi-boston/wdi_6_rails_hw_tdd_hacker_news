<h1>Create A New Submission</h1>

<%= form_for @submission do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<%= f.label :url %>
	<%= f.text_field :url %>
	<%= f.submit 'Create Submission' %>
<% end %>