<h1>All Posts</h1>
<% @posts.each do |post| %>
	<h2><%= link_to post.description, "#{post.link}" %></h2>
	<%= link_to "^" , post_upvote_path(post.id) %>
	<%= post.upvotes %>
	<%= link_to 'Leave a comment', new_post_comment_path(post) %>
<% end %>
